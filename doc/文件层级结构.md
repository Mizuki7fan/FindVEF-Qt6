src/
├── main.cpp
├── core/                    # 核心业务逻辑
│   ├── mesh/               # 网格相关
│   │   ├── MeshData.h/cpp          (原 CGALMeshInterface)
│   │   ├── MeshMetadata.h/cpp      (保持不变)
│   │   └── MeshOperations.h/cpp    (从CGALMeshInterface分离出的操作)
│   └── io/                 # 文件输入输出
│       ├── FbxLoader.h/cpp         (原 FbxIOInterface)
│       ├── MeshLoader.h/cpp        (通用网格加载器)
│       └── FileFormats.h           (文件格式定义)
├── ui/                     # 用户界面层
│   ├── windows/            # 窗口
│   │   └── MainWindow.h/cpp        (保持不变)
│   ├── widgets/            # 自定义控件
│   │   ├── FindVEFController.h/cpp (保持不变)
│   │   └── Controller.h/cpp        (保持不变)
│   └── panels/             # 面板组件
│       ├── FindVEFPanel.h/cpp      (保持不变)
│       └── Panel.h/cpp             (保持不变)
├── graphics/               # 图形渲染相关
│   ├── rendering/          # 渲染逻辑
│   │   └── OpenGLRenderer.h/cpp    (从Panel中分离)
│   └── utils/              # 图形工具
│       └── MergeSnapshot.h         (保持不变)
├── utils/                  # 通用工具
│   ├── ErrorHandler.h/cpp          (保持不变)
│   └── FindVEFHandler.h/cpp        (保持不变)
└── 需求管理.md
